import React from 'react'

const Details = ({id}) => {
    useEffect(() => {
      first
    
      return () => {
        second
      }
    }, [id])
    
  return (
    <div>Details</div>
  )
}

export default Details